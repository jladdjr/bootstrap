set from = "jladd@redhat.com"
set sendmail = "/usr/bin/msmtp -a redhat"
set sendmail_wait = 0
set mbox = "+redhat.com/archive"
set postponed = "+redhat.com/drafts"
set signature = ~/.mutt/sig-redhat
set sig_on_top = yes
unset record
