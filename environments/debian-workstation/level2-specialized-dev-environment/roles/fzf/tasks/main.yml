- name: Install fzf
  apt:
    name:
      - fzf
    state: latest
    update_cache: true
  become: true

- name: Source bindings for fzf
  lineinfile:
    path: "{{ lookup('env', 'HOME') }}/.bashrc"
    line: "source /usr/share/doc/fzf/examples/key-bindings.bash"
