## Zoom Setup Instructions

1. Download Zoom deb package from [Zoom Download Page](https://zoom.us/download)
2. Install with:
  `sudo apt install ./zoom_amd64.deb`
3. Should be able to open zoom using bemenu-run with 'zoom'
